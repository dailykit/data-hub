import {
    LabelTemplate
} from '../../../models/settings/index';
import dummy from 'mongoose-dummy';

const fake_labelTemplate_data = () => dummy(LabelTemplate);

export default fake_labelTemplate_data