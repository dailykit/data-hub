import {
    SupplierItem
} from '../../../models/inventory/index';

import dummy from 'mongoose-dummy';

const fake_supplierItem_data = () => {} //dummy does not support Decimal128, therefore has to be resolved in some other way

export default fake_supplierItem_data